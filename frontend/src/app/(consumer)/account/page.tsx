import { PlaceholderPage } from '@/shared/components/feedback/placeholder-page';

export default function AccountPage() {
  return (
    <PlaceholderPage
      title="Account"
      description="Profile settings and preferences arrive in a later milestone."
    />
  );
}
